<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- GLSurfaceView for OpenGL ES rendering -->
    <android.opengl.GLSurfaceView
        android:id="@+id/gl_surface_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Debug Button Overlay - Top-left, below status bar -->
    <LinearLayout
        android:id="@+id/debug_button_panel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginStart="10dp"
        android:layout_marginTop="50dp"
        android:orientation="vertical"
        android:padding="4dp"
        android:visibility="gone">

        <Button
            android:id="@+id/btn_debug_console"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Console"
            android:textSize="12sp"
            android:padding="4dp" />

    </LinearLayout>

    <!-- Debug Toggle Button - Top-left corner -->
    <Button
        android:id="@+id/btn_debug_toggle"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:layout_gravity="top|start"
        android:layout_marginStart="10dp"
        android:layout_marginTop="10dp"
        android:text="D"
        android:textSize="12sp"
        android:background="#333333"
        android:textColor="#FFFFFF"
        android:alpha="0.5" />

    <!-- Debug Menu FAB - Bottom-right corner -->
    <Button
        android:id="@+id/btn_debug_fab"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:text="BUG"
        android:textSize="9sp"
        android:textColor="#FFFFFF"
        android:backgroundTint="#BB4444"
        android:elevation="8dp" />

</FrameLayout>
